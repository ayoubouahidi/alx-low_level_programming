#include<stdio.h>
/**
 * main - Entry point
 * description: prints the alphabet in lowercase, followed by a new line
 * Return : Always 0
 */
int main(void)
{
	putchar(65);
	putchar('\n');
	return (0);
}
